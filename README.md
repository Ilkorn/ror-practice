# ror-practice
My ruby practice repo

## Most usable command here:

###*Common*
`rails server`

###*Generate*
`rails generate scaffold ITEM`


###*DB*

`rake db:migrate`
`rake db:seed`
`rake db:rollback`
